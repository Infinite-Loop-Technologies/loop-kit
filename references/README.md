# references

Use this folder only for durable support material that does not belong in a package and does not fit cleanly in `ARCHITECTURE.md`.

Rules:

- prefer obvious filenames like `project-loom.md` or `reference-instantdb.md`
- prefer one useful file over multiple cross-linked files
- keep tasks in `CHECKLIST.md`, not here
- delete stale docs instead of archiving by default
