{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Default capability for the Forge desktop main window.",
    "windows": ["main"],
    "permissions": ["core:default"]
}
