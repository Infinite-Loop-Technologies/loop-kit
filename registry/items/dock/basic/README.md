Basic Dock is an installable source item.

It is intentionally small and demonstrates the intended split:

- package code supplies stable headless boundaries
- registry item code owns app-facing shell and chrome
- downstream apps are expected to edit this code directly
