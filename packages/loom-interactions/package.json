{
    "name": "@loop-kit/loom-interactions",
    "version": "0.1.0",
    "description": "Headless interaction coordination managers and React bindings for Loom.",
    "type": "module",
    "sideEffects": false,
    "main": "./src/index.ts",
    "module": "./src/index.ts",
    "types": "./src/index.ts",
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "import": "./src/index.ts",
            "default": "./src/index.ts"
        },
        "./package.json": "./package.json"
    },
    "files": ["src", "README.md"],
    "scripts": {
        "build": "tsc -p tsconfig.build.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "clean": "bun -e \"import { rmSync } from 'node:fs'; rmSync('dist', { recursive: true, force: true });\"",
        "test": "bun test",
        "prepack": "bun run clean && bun run build"
    },
    "dependencies": {
        "react": "^19.1.0"
    },
    "peerDependencies": {
        "react-dom": "^18.2.0 || ^19.0.0"
    },
    "devDependencies": {
        "@types/react": "^19.1.2",
        "typescript": "^5.9.3"
    }
}
