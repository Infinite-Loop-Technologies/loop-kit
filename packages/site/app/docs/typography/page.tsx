import { Prose } from '@/registry/new-york/ui/prose/prose';

export default function TypographyPage() {
    return (
        <Prose>
            <h1>test prose</h1>
            <p>This would be so sick if it worked.</p>
        </Prose>
    );
}
