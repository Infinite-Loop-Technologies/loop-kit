# @loop-kit/common-react

React adapters for foundational `@loop-kit/common` primitives.

- `useStore`
- `useStoreSelector`
- reusable required-context helpers

This package should stay small and domain-free.
