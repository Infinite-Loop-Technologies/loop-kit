# @loop-kit/loom-pack-dock

Dock UI pack for Loom.

This package owns the higher-level dock React surfaces, scoped registrations, drag/drop bridge, and demo workbench. The lower-level headless dock model, commands, and policies remain in `@loop-kit/dock`.
