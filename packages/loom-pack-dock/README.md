# @loop-kit/loom-pack-dock

Dock UI pack for Loom.

This package owns the higher-level dock React surfaces and demo workbench. The lower-level Graphite-backed dock model and geometry helpers remain in `@loop-kit/dock`.
