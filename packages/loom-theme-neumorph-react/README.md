# @loop-kit/loom-theme-neumorph-react

Neumorphic Loom theme.

This layer focuses on soft contour-heavy tokens plus a `Button` implementation override to prove primitive implementation replacement without changing public props.
