# @loop-kit/ui

Shared UI primitives used by `packages/site` and Moon-generated apps.

- Source of truth lives in `packages/ui/src`.
- `packages/site/scripts/sync-registry-ui.mjs` copies selected files into
  `packages/site/registry/new-york/ui` before `shadcn build`.
