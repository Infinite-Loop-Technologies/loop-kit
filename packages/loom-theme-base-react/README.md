# @loop-kit/loom-theme-base-react

Base theme layer for Loom React.

It provides the default token set, default recipes, default semantic icon set,
and the baseline primitive implementation map that other React themes inherit
from.
