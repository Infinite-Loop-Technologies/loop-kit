# Codex

- Use Moon for task orchestration (`moon run`, `moon ci`).
- Tool versions are managed by Proto (`.prototools`).
- Prefer Moon generators in `templates/` (`moon generate <id>`).
- Moon docs for LLMs: https://moonrepo.dev/llms.txt
