# Loop Kit Examples

Examples are intentionally small proof surfaces for package behavior.

- `simple`: Bun + React smoke test for `@loop-kit/interaction`.
- `workbench`: richer dock and interaction showcase with client/server/shared
  boundaries.

Run the main demo with:

```bash
bun dev
```
