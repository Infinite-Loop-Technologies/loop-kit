# Fixtures

`fixtures/` is reserved for shared data that is reused by repo-level tests or multiple packages.

Do not put one-off sample files here. Keep fixtures local until at least two different places genuinely need the same data.
