# Copy to .env.release and load it with dotenvx for publish flows.

# Required when LOOP_PUBLISH_DRY_RUN=false
NODE_AUTH_TOKEN=

# Optional publish controls
LOOP_PUBLISH_TAG=latest
LOOP_PUBLISH_DRY_RUN=true
LOOP_PUBLISH_SKIP_CHECKS=false
