# Copy to .env.release. `bun --env-file=.env.release tools/publish-packages.ts`
# loads it for the publish flow.

# Required for publish
NODE_AUTH_TOKEN=

# Optional legacy alias if you prefer it over NODE_AUTH_TOKEN
# NPM_TOKEN=
