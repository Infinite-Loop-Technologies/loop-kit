$schema: 'https://moonrepo.dev/schemas/toolchain.json'

node:
  version: '22.20.0'
  packageManager: 'pnpm'
  syncPackageManagerField: true

pnpm:
  version: '10.15.1'
  installArgs:
    - '--frozen-lockfile'
